spring:
  application:
    name: config-test

config:
  info: config-test-1.0-nice

eureka:
  instance:
    hostname: config-test-service
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
